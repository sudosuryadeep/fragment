import "./globals.css";

export const metadata = {
  title: "Fragment",
  description: "Username auction",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}